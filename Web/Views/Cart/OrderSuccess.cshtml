@*@model Web.ViewModels.OrderViewModel

@{
    ViewData["Title"] = "OrderSuccess";
}*@

@*<h1>OrderSuccess</h1>
<div class="row">
    <div class="col-sm-12 col-md-4">
        <div class="card">

            <h3 class="card-header">
                @Html.LabelFor(modelItem => modelItem.Order.Id) @Html.DisplayFor(modelItem => modelItem.Order.Id)
            </h3>

            <div class="card-body">

                <div class="card-text">
                    <div>
                        <div>
                            @Html.LabelFor(modelItem => modelItem.Order.Date)
                            @Model.Order.Date.ToString("dddd, dd MMMM yyyy klHH:mm")
                        </div>
                    </div>

                    <div>
                        <h3>User</h3>
                        <address>
                            @Html.DisplayFor(modelItem => modelItem.User.FirstName) @Html.DisplayFor(modelItem => modelItem.User.LastName)
                            <br />
                            @Html.LabelFor(modelItem => modelItem.User.PhoneNumber): @Html.DisplayFor(modelItem => modelItem.User.PhoneNumber)
                        </address>
                    </div>
                    <h4>Contact info</h4>
                    <address>
                        <div>@Html.DisplayFor(modelItem => modelItem.User.Address)</div>
                        <div>@Html.DisplayFor(modelItem => modelItem.User.ZipCode)</div>
                        <div>@Html.DisplayFor(modelItem => modelItem.User.City)</div>
                    </address>
                </div>
            </div>
        </div>
    </div>
    <div class="col-sm-12 col-md-8">
        <table class="table table-responsive-sm">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => new OrderRow().Product.Name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => new OrderRow().Product.Price)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => new OrderRow().Quantity)
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Order.OrderRows)
                {
                    <tr>*@
                        @*<td>
                            @Html.DisplayFor(modelItem => item.Product.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Product.Price)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Quantity)
                        </td>
                    </tr>
                }
            </tbody>
            <tfoot>
                <tr>
                    <td colspan="3">
                        @Html.LabelFor(modelItem => modelItem.Order.TotalPrice)
                        @Html.DisplayFor(modelItem => modelItem.Order.TotalPrice)
                    </td>
                </tr>
            </tfoot>
        </table>
    </div>
</div>*@